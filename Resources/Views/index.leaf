<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Moa - Jira 회고 리포트</title>
    <style>
        body {
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif;
            background-color: #f5f5f7;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
        }
        .container {
            background: white;
            padding: 2rem;
            border-radius: 12px;
            box-shadow: 0 4px 6px rgba(0,0,0,0.1);
            width: 100%;
            max-width: 400px;
        }
        h1 {
            text-align: center;
            color: #1d1d1f;
            margin-bottom: 1.5rem;
        }
        .form-group {
            margin-bottom: 1rem;
        }
        label {
            display: block;
            margin-bottom: 0.5rem;
            font-weight: 500;
            color: #1d1d1f;
        }
        input {
            width: 100%;
            padding: 0.8rem;
            border: 1px solid #d2d2d7;
            border-radius: 8px;
            font-size: 1rem;
            box-sizing: border-box;
        }
        .help-text {
            font-size: 0.8rem;
            color: #86868b;
            margin-top: 0.25rem;
        }
        button {
            width: 100%;
            padding: 1rem;
            background-color: #0071e3;
            color: white;
            border: none;
            border-radius: 8px;
            font-size: 1rem;
            font-weight: 600;
            cursor: pointer;
            margin-top: 1rem;
        }
        button:hover {
            background-color: #0077ed;
        }
        .token-link {
            font-size: 0.8rem;
            color: #0071e3;
            text-decoration: none;
            float: right;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Moa 회고 리포트</h1>
        
        #if(savedEmail):
        <div class="form-group" style="text-align: center; margin-bottom: 2rem;">
            <p style="font-size: 1.1rem; color: #1d1d1f;">👋 반가워요, <strong>#(savedEmail)</strong>님!</p>
            <p class="help-text">저장된 계정으로 바로 리포트를 생성할까요?</p>
            
            <form action="/moa/collect" method="POST">
                <div style="margin-bottom: 1rem; text-align: left;">
                    <label for="quick-year" style="font-size: 0.9rem; color: #666;">회고 연도</label>
                    <input type="number" id="quick-year" name="year" value="2025" style="padding: 0.5rem;">
                </div>
                <button type="submit" style="margin-bottom: 1rem;">🚀 바로 리포트 보기</button>
            </form>
            
            <button onclick="document.getElementById('login-form').style.display='block'; this.style.display='none';" 
                    style="background-color: #f5f5f7; color: #1d1d1f; border: 1px solid #d2d2d7;">
                다른 계정으로 로그인
            </button>
        </div>
        #endif

        <form action="/moa/collect" method="POST" id="login-form" #if(savedEmail): style="display: none;" #endif>
            <div class="form-group">
                <label for="year">회고 연도</label>
                <input type="number" id="year" name="year" value="2025" required>
            </div>

            <div class="form-group">
                <label for="email">Jira 이메일</label>
                <input type="email" id="email" name="email" placeholder="example@kurlycorp.com" required>
            </div>

            <div class="form-group">
                <label for="token">
                    Jira API 토큰
                    <a href="https://id.atlassian.com/manage-profile/security/api-tokens" target="_blank" class="token-link">토큰 발급받기 &rarr;</a>
                </label>
                <input type="password" id="token" name="token" placeholder="API 토큰을 입력하세요" required>
                <div style="margin-top: 0.5rem; font-size: 0.8rem; color: #86868b;">
                    <p style="margin: 0.2rem 0;">⚠️ 토큰은 <strong>생성 직후에만 복사</strong>할 수 있습니다. (다시 조회 불가)</p>
                    <p style="margin: 0.2rem 0;">🔄 토큰을 잊어버렸거나 만료된 경우, 새로 생성해서 입력해주세요.</p>
                    <p style="margin: 0.2rem 0;">🔒 입력한 토큰은 브라우저에 안전하게 저장됩니다.</p>
                </div>
            </div>

            <div class="form-group">
                <label for="spaceKey">
                    Confluence Space Key (Optional)
                </label>
                <input type="text" id="spaceKey" name="spaceKey" placeholder="예: /wiki/spaces/appr/overview 👉 appr" #if(savedSpaceKey): value="#(savedSpaceKey)" #endif>
            </div>

            <button type="submit">리포트 생성하기</button>
        </form>
    </div>
</body>
</html>
